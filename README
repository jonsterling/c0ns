To get started with this repository, clone it:

    git clone --recursive git@github.com:jonsterling/15-712-project.git


When you pull, be sure to also update submodules:

    git submodule udpate --init --recursive
