#use <conio>
#use "message.c1"
#use "testcases.c0"

int main() {
//    struct header* hd;
    struct message* m=test_case1();
//print("hello world\n");
 //   hd = alloc(struct header);
 	  //m = alloc(struct message);
//  	  m->header = hd;

test_roundtrip(m);
    
    return 0;
}