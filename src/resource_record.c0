#use "types.c0"
#use "class.c0"

struct resource_record {
    string name; (* a domain name, subject to compression *)
    type_ type_;
    class class;
    int ttl;
    int rd_length;
    char[] rdata;
};